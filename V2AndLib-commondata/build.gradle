apply plugin: 'java'

sourceCompatibility = 1.6
version = '1.2.1'
jar {
    manifest {
            attributes 'Implementation-Title': 'V2AndLib-commondata', 'Implementation-Version': version
    }
}

sourceSets {
    main {
	java {
    	    srcDir 'src'
        }
    }
}
