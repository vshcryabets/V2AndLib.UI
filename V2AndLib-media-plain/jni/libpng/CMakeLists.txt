CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

AUX_SOURCE_DIRECTORY( . LIB_SOURCES )
LIST(REMOVE_ITEM LIB_SOURCES "./example.c" )
MESSAGE(STATUS "SRCS=${LIB_SOURCES}")
ADD_LIBRARY( libpng STATIC ${LIB_SOURCES} )
